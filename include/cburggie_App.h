#ifndef _CBURGGIE_APP_H
#define _CBURGGIE_APP_H

namespace cburggie
{
	class App
	{
/*
		private:
			bool Running;
			SDL_Window * window;
			SDL_Renderer * renderer;

		protected:
			Logger logger;

		public:
			App();
			~App();
			OnInit();
			OnExecute();
			OnCleanup();

		protected:
			OnEvent(SDL_Event * Event);
			OnLoop();
			OnRender();
*/
	};
}

#endif
